# Environment Configuration Example
# Copy this file to .env.local and add your actual values

# Google Gemini AI API Key
# Get your key from: https://ai.google.dev/
# This is optional - users can also configure their API key in the Settings panel
API_KEY=your_gemini_api_key_here

# Notes:
# - Never commit your actual .env.local file to version control
# - The .gitignore file is configured to exclude .env.local
# - Users can alternatively configure API keys in the app's Settings panel
# - Environment variable keys are only recommended for development or private deployments
